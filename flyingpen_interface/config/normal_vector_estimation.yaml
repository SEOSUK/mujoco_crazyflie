/**:
  ros__parameters:
    normal_estimator_method: "normal_force_based" # overridden by su_params.yaml when needed; "direction", "normal_force_based", "normal_velocity_PE_based", "normal_mujoco_measured", "None"
    publish_hz: 100.0  # normal estimator publish/update rate [Hz]

    flip_measured_force: false  # follow firmware sign by default; enable only for an explicit experimental frame flip

    # "force_sensor", "momentum_observer_2nd_order",
    # "momentum_observer_2nd_order_consistency"
    force_observation_source: "momentum_observer_2nd_order_consistency"
